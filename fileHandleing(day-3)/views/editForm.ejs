<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Add New Book</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: #f8f9fa;
    }
    .form-container {
      max-width: 600px;
      background: white;
      border-radius: 15px;
      padding: 40px;
      margin: 60px auto;
      box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);
    }
    .form-title {
      font-weight: 600;
      margin-bottom: 30px;
      color: #343a40;
      text-align: center;
    }
  </style>
</head>
<body>

  <div class="container">
    <div class="form-container">
      <h2 class="form-title">📚 Update Book</h2>
      <form action="/updateBook" enctype="multipart/form-data" method="post">
        <div class="mb-3">
            <label for="bookName" class="form-label" hidden>Book Name</label>
            <input type="text" class="form-control" name="id" hidden value="<%=data.id%>" placeholder="e.g. Atomic Habits" required>
          </div>
        <div class="mb-3">
          <label for="bookName" class="form-label">Book Name</label>
          <input type="text" class="form-control" name="bookName" value="<%=data.bookName%>" placeholder="e.g. Atomic Habits" required>
        </div>

        <div class="mb-3">
          <label for="authorName" class="form-label">Author Name</label>
          <input type="text" class="form-control" name="authorName" value="<%=data.authorName%>" placeholder="e.g. James Clear" required>
        </div>

        <div class="mb-3">
          <label for="price" class="form-label">Price (₹)</label>
          <input type="text" class="form-control"  name="price"  value="<%=data.price%>" placeholder="e.g. 399" required>
        </div>

        <div class="mb-3">
          <!-- <label for="price" class="form-label">Image</label> -->
          <input type="file" class="form-control"  name="image"  placeholder="e.g. 399" >
          <img src="<%=data.image%>" alt="">
        </div>


       <button type="submit" class="btn btn-success"> Update Book</button>
        </div>
      </form>
    </div>
  </div>

</body>
</html>
